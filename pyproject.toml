[project]
name = "dugout-atlas"
version = "1.11"
description = "PyQt6 MLB live gameday and sabermetrics desktop application"
requires-python = ">=3.12"

[tool.pytest.ini_options]
testpaths = ["tests"]
addopts = "-q"
